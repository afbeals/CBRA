// Local
import reducer from './reducer';
import fetchingPersistConfig from './persist';

import * as selectors from './selectors';

export { selectors, fetchingPersistConfig };

export default reducer;
